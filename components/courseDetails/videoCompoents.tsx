"use client"

import { UserCoursePart } from "@prisma/client"
import { Play } from "lucide-react"
import { useState } from "react"

interface Props{
    initialData:UserCoursePart | null
    
}
export const VideoComponent=({initialData}:Props)=>{
    return(
    <div className="">m</div>
    )
}